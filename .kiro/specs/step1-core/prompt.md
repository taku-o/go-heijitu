/kiro-spec-requirements
docs/planningにプロジェクトの開発計画の資料が置いてあります。
まずその資料を読み込み、
次に、開発計画ステップ1の開発を進めていきます。
>  ┌──────────┬────────────────────────────────────────────────┐
>  │ ステップ │                      内容                      │
>  ├──────────┼────────────────────────────────────────────────┤
>  │ Step 1   │ プロジェクト初期化・コア型・IsBusinessDay まで │
>  ├──────────┼────────────────────────────────────────────────┤
>  │ Step 2   │ holidayjp プロバイダー + 残り全APIの実装       │
>  ├──────────┼────────────────────────────────────────────────┤
>  │ Step 3   │ 内閣府CSVプロバイダー                          │
>  ├──────────┼────────────────────────────────────────────────┤
>  │ Step 4   │ Google Calendar APIプロバイダー                │
>  ├──────────┼────────────────────────────────────────────────┤
>  │ Step 5   │ example・GoDoc・README（ドキュメント整備）     │
>  └──────────┴────────────────────────────────────────────────┘

作業用のgitブランチを作成後、
ステップ1の要件定義書を作成してください。

-------------

/kiro-validate-gap step1-core

このプロジェクトではエラーが発生したら即座に投げる。
エラーを握りつぶすようなことはしない。フォールバックなども不要。

/kiro-approve-req step1-core

jj new

-------------

/kiro-spec-design step1-core

/kiro-validate-design step1-core

  Suggestion: 依存方向図を以下に修正する：
  Holiday, MonthDay（値オブジェクト）
    ↑ (HolidayProvider, config がそれぞれ依存)
  HolidayProvider（インターフェース） ← calendar.go が直接依存
  config（内部設定型）             ← option.go が依存
    ↑
  option.go
    ↑
  calendar.go（BusinessCalendar）

Suggestion: File Structure Plan に package heijitu の記載を追加する。

/kiro-approve-design step1-core

jj new

-------------

/kiro-spec-tasks step1-core

/kiro-approve-task step1-core

/kiro-review-spec step1-core

見つかった問題、簡単に直せるものは直して。

仕様周り。

2月29日など境界値の扱い、は適当に仕様を決めて。

これも対応。適当に仕様を決める。
> HolidayName が祝日の場合の返り値が未記載。

jj new

jj bookmark set feature/step1-core-implementation -r @-

git switch feature/step1-core-implementation
pull request作成

-------------

https://github.com/taku-o/go-heijitu/pull/2
/review 2

jj new

-------------

takt --task "/kiro-impl step1-core 1"

/code-review


こちらを修正して。
  ┌────────┬────────────────────┬──────────────────────────────────────────────────────────────────────┐
  │ 深刻度 │      ファイル      │                                 内容                                 │
  ├────────┼────────────────────┼──────────────────────────────────────────────────────────────────────┤
  │ 中     │ design.md:380      │ path.Ext()（pathパッケージ）を指示しているが許可依存は path/filepath │
  │        │                    │  のみ。Windowsでバックスラッシュパスが壊れる。                       │
  ├────────┼────────────────────┼──────────────────────────────────────────────────────────────────────┤
  │ 中     │ requirements.md:71 │ 要件4.3の「New()に渡す」表現が (Option, error)                       │
  │        │                    │ シグネチャと矛盾し、コンパイルエラーを誘導。                         │
  ├────────┼────────────────────┼──────────────────────────────────────────────────────────────────────┤
  │        │                    │ extraHolidays                                                        │
  │ 低     │ design.md:301      │ フィールド名が意味的に誤り（除外日付なのに「祝日」）。extraExcluded  │
  │        │                    │ と命名を揃えるべき。                                                 │
  └────────┴────────────────────┴──────────────────────────────────────────────────────────────────────┘

/kiro-review-spec step1-core

これらの問題を修正してください。
  1. タスク 2.1 の _Requirements: から 1.4 が欠落
    - _Requirements: 1.1, 1.2, 1.3_ とあるが 1.4（バリデーション非実装・直接比較）が未記載
    - タスク 5.1 では 1.4 が記載されており不統一。実装タスク側にも記載すべき
  2. タスク 5.3 に _Depends: 4.3_ がない
    - (P) マーカーが付いているが、5.3 は IsBusinessDay 実装（4.3）完了後でなければ意味をなさない
    - 5.1 / 5.2 は真の並列実行が可能だが、5.3 は 4.3 への暗黙依存がある
  3. タスク 1.1 の要件マッピングが意味的に不正確
    - _Requirements: 6.1_（YAML パース）はモジュール初期化の「目的」ではなく「依存の根拠」
    - 本質的な問題はないが、読み手が混乱する可能性あり

  1. コンポーネント一覧で要件 1.4 が欠落
    - MonthDay の要件カバレッジ欄が「1.1, 1.2, 1.3」のみで 1.4（バリデーションなし・直接比較）が未記載
    - Requirements Traceability 表には 1.4 が記載されており不統一
  2. コンポーネント一覧で要件 3.7 が欠落
    - HolidayProvider の要件カバレッジ欄が「3.1, 3.6」のみで、3.7（エラー伝播）が未記載
    - 同様に Requirements Traceability 表には 3.7 が記載されており不統一

  対応推奨アクション
  - docs/planning/ は全体計画書として現状の記述が妥当

/kiro-review-spec step1-core

こちら修正してください
  1. Requirement 3.6 の from > to の境界動作が未定義
    - HolidaysBetween で from > to を渡した場合の振る舞い（空スライス返却かエラーか）が記載されていない
    - 実装者が独自に決定する余地があり、後続ステップでの利用で不整合が生じる可能性

/kiro-validate-impl step1-core 1

/simplify-loop

/kiro-complete-tasks step1-core 1

jj new

-------------

takt --task "/kiro-impl step1-core 2"
/kiro-validate-impl step1-core 2
/code-review

これらを修正して
  ┌────────┬───────────────────────────────────────────────────────────────────────────────────────────┐
  │ 深刻度 │                                           内容                                            │
  ├────────┼───────────────────────────────────────────────────────────────────────────────────────────┤
  │ 低     │ monthday_test.go:71 —                                                                     │
  │        │ テストケース名と意図が実際のコードと乖離している（削除または正しい実装に修正推奨）        │
  ├────────┼───────────────────────────────────────────────────────────────────────────────────────────┤
  │ 低     │ holiday_test.go:27 — Goコンパイラ保証のみを検証する冗長なテスト（削除推奨）               │
  └────────┴───────────────────────────────────────────────────────────────────────────────────────────┘

/simplify-loop
/kiro-complete-tasks step1-core 2
jj new

-------------

takt --task "/kiro-impl step1-core 3"
/kiro-validate-impl step1-core 3
/code-review

こちら修正してください
  ┌──────┬────────────────┬──────────────────────────────────────────────────────────────────────────┐
  │ 深刻 │    ファイル    │                                   内容                                   │
  │  度  │                │                                                                          │
  ├──────┼────────────────┼──────────────────────────────────────────────────────────────────────────┤
  │ 中   │ config_test.go │ json:"month" タグ追加後もテストが大文字キー {"Month"}                    │
  │      │ :82            │ を使い続け、コメントが誤った説明をしている                               │
  ├──────┼────────────────┼──────────────────────────────────────────────────────────────────────────┤
  │ 低   │ provider_test. │ if p == nil は到達不能な死コード（インターフェース充足チェックは既に18行 │
  │      │ go:53          │ 目のコンパイル時検査で行われている）                                     │
  ├──────┼────────────────┼──────────────────────────────────────────────────────────────────────────┤
  │ 低   │ config_test.go │ 要件番号のコメント誤記（6.3 → 6.4）                                      │
  │      │ :132           │                                                                          │
  └──────┴────────────────┴──────────────────────────────────────────────────────────────────────────┘

/simplify-loop
/kiro-complete-tasks step1-core 3
jj new

-------------

takt --task "/kiro-impl step1-core 4"

takt --task "/kiro-impl step1-core 4
必要ならテストを修正して良い。"

/kiro-review-feature step1-core 4
jj new

-------------

takt --task "/kiro-impl step1-core 5
必要ならテストを修正して良い。"

/kiro-review-feature step1-core 5
jj new

-------------

jj-merge feature/step1-core-implementation
/commit-push-pr-update

/review 2

これ対応して
  1. MonthDay に GoDoc が未記載（monthday.go:5）
  // MonthDay は年をまたいで有効な月日を表す値オブジェクト。
  type MonthDay struct {
  1. パッケージの公開型には GoDoc があると Step 5 のドキュメント整備が容易になる。
  2. Holiday フィールドに GoDoc が未記載（holiday.go:5-8）
  Date フィールドが full
  time.Time（日時＋タイムゾーン）である理由・想定時刻の説明があると後続の実装者が混乱しない




